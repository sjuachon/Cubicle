  {{myUser}}
       {{!-- {{#if myUser.username !== ""}}
          <p>{{myUser.username}} is logged in.</p>  
      {{else}}    
          <p>Your are logged in as a guest.</p>
      {{/if}} --}}

  
  <h1>Browser</h1>
     {{>search}}
      {{#each cubes}}
      <div class="cube">
        <img class="cube" src="{{imageUrl}}">
        <p class="name">{{name}}</p>
        <p><span>Difficulty level:</span>{{difficultyLevel}}</p>
        <a class="btn" href="/details/{{_id}}"> Details</a>
      </div>
    
      {{/each}}
      
      